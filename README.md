# Fibra+ Hub

## Restauração atual
- Pasta `api/` restaurada com ponte para `server.js`.
- Estrutura do projeto preservada sem criar HTML novo.
- Removidas tentativas visuais antigas da Efí.
- Configuração Efí mantida no backend local em `data/efi-config.json`, sem Supabase.
- Veja `RELATORIO_RESTAURACAO_API_EFI.md` para detalhes das rotas e chamadas `/api`.
